export class User {


     constructor(Username, FirstName, LastName, Email, Role) {
         this.Username = Username;
         this.FirstName = FirstName;
         this.LastName = LastName;
         this.Email = Email;
         this.Role = Role;
     }

}